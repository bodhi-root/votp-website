// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://bodhi-root.github.io',
  //base: '/votp-website', // Uncomment this line if deploying to GitHub Pages without a custom domain mapping to it
});
